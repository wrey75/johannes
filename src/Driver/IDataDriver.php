<?php

/**
 * The Data driver will be invoked to get or put some
 * information in the database. This is just a basic
 * class because the implementation is not really
 * offered. Basically, the driver should be able to
 * save both on SQL or document databases (NoSQL
 * databases like MongoDB or Redis). This is because
 * of some possible issue performances, the driver
 * is NOT delivered in version 1 of Johannes.
 * 
 * @author wrey75
 *
 */
interface IDataDriver {

    
}

